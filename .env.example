# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Google Gemini AI Configuration (Optional - for AI-powered summary generation)
# Get your API key from: https://aistudio.google.com/app/apikey
# Uses gemini-2.5-flash-lite model (fast, efficient, and latest)
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# GitHub Public Token (Optional but HIGHLY RECOMMENDED for public portfolio sharing)
# This token is used when non-logged-in users view portfolios to avoid rate limiting
# Create a token at: https://github.com/settings/tokens/new
# Required scopes: public_repo (read-only access to public repositories)
# This prevents "API rate limit exceeded" errors on shared portfolio links
VITE_GITHUB_PUBLIC_TOKEN=your_github_token_here

# Optional: Add your actual values to .env (gitignored)
